{
  "name": "uni-2",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=18.20.0"
  },
  "packageManager": "pnpm@11.26.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && wrangler pages deploy dist --project-name formulacion-evaluacion-proyectos-ti --branch main",
    "sync": "astro build && wrangler pages deploy dist --project-name formulacion-evaluacion-proyectos-ti --branch main && git add . && git commit -m 'update: cambios y despliegue' && git push",
    "astro": "astro"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^7.3.3",
    "bpmn-js": "^18.29.1",
    "tailwindcss": "^4.3.3"
  },
  "allowScripts": {
    "esbuild": true
  },
  "devDependencies": {
    "wrangler": "^4.139.0"
  }
}
